// Basic placeholder widget test.
//
// The default counter-app test that Flutter generates doesn't match this
// project (EduVerse AI has no counter/+ button), so it's replaced here with
// a minimal smoke test that just confirms the test suite compiles and runs.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    expect(true, isTrue);
  });
}